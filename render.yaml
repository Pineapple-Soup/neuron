services:
  - type: web
    name: neuron
    runtime: node
    buildCommand:
    startCommand: npx y-websocket --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
    ignorePaths:
      - src
    plan: free
